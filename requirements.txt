streamlit==1.56.0
langgraph==1.1.6
langchain==1.2.15
langchain-core==1.2.26
langchain-openai==1.1.12
langchain-google-genai==4.2.1
python-dotenv==1.2.2
requests==2.33.1

# CAD / Geometry
cadquery==2.7.0
# 注意：不要在云端硬 pin cadquery-ocp。
# Streamlit Cloud 默认可能用较新 Python（如 3.14），会导致 ocp 无 wheel 而安装失败。
# 由 cadquery 自行选择兼容的 cadquery-ocp 版本（配合 runtime.txt 固定 python-3.11）。

# 3D preview (STL -> glTF -> three.js viewer)
pyvista==0.47.2
vtk==9.3.1
nest-asyncio2==1.7.2
